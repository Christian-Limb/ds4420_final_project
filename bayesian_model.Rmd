---
title: "Final Project Bayesian Regression Model"
author: "Yuqi Chen"
output: html_notebook
---


```{r}
df <- read.csv("ngshistory.csv")
df$Week.ending <- as.Date(df$Week.ending)
df
```
```{r}
df_sp <- read.csv("Henry_Hub_Natural_Gas_Spot_Price.csv")
df_sp$Week.of <- as.Date(df_sp$Week.of, format="%m/%d/%Y")
df_sp
```
```{r}
mask <- as.Date("2010-01-01") <= df_sp$Week.of & df_sp$Week.of <= as.Date("2026-02-06")

data <- cbind(
  df_sp[mask, "Henry.Hub.Natural.Gas.Spot.Price.Dollars.per.Million.Btu", drop = FALSE],
  df[mask, "Total.Lower.48", drop = FALSE])
data
```


